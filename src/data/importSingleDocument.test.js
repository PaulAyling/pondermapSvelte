import {singleDocument} from './importSingleDocument'
const util = require('util')


describe.only('import test', () => {
    it('1. View single docuemnts', () => {
    // console.log(util.inspect(singleDocument,{ depth: null }))
      expect('').toStrictEqual('');
    });
   
  });