// Function that allows cloning of other users content


cloneComponent = () =>{
    console.log("This is used to clone one com[plenet from another view of the docment")
}
// THis function will live in the createDocument section (not created yet)
cloneEntireView = () =>{
    console.log("Complelt clone of another users view of a document")
}

export{cloneComponent, cloneEntireView}